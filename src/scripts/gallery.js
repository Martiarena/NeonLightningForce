import GLightbox from 'glightbox';

document.addEventListener("DOMContentLoaded", () => {
	"use strict";
    // Galería de imágenes
	GLightbox({
		loop: true,
		width: '100%',
		height: '100%',
	});
});